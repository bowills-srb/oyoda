# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
.venv311/
venv/
.migration_venv/
*.egg-info/
dist/
build/

# Node
node_modules/
npm-debug.log*

# Environment — NEVER commit secrets
.env
.env.*
!.env.example
# .env.local is the committed local-dev template (stubs only, no real secrets).
# Real/secret overrides belong in .env (above), which is gitignored.
!.env.local

# Docker volumes / data
pgdata/
redis-data/

# Compiled dashboard (regenerate with: node scripts/build-dashboard.js)
app/static/dashboard/oyvoda-dashboard.js

# IDE
.DS_Store
.idea/
.vscode/
*.swp
~$*.xlsx

# Logs
*.log
logs/

# Local script outputs
scripts/output/

# Local operator spreadsheets / exports at repo root
/*.xlsx

# Coverage
.coverage
htmlcov/

# Alembic
alembic/versions/*.pyc
ENET_API_Package/
